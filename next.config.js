/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['image.shutterstock.com', 'avatars.githubusercontent.com']
  }
}

module.exports = nextConfig
